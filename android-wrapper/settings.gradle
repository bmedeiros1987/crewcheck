pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS); repositories { google(); mavenCentral() } }
rootProject.name = 'CrewCheckAndroid'
include ':app'

include ':wear'
include ':watchface'

include ':lifecompanion'

// Isolated Android Auto prototype. Keep out of the production APK until car-category review is complete.
include ':auto'
