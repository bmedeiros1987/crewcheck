# CrewCheck TV 0.1.7 — official horizontal identity and separated broadcast views

Owner requested the approved broadcast design, split to avoid dense screens, exact Crewtopia spelling, original Cirium and Bruno Medeiros Tecnologia marks, and a new LG IPK. This preview retains navy/cyan/violet surfaces, task-specific cards, optional screen care, manual pause and the four local MP3 tracks.

Explicit preview composition: run `node scripts/tv-broadcast-prepare.mjs` before TV type/build. Its checked/idempotent anchors update only the TV presentation entry/channel composition, not canonical/auth/provider/backend logic. It is not applied to the production web/mobile build. The CI artifact includes the prepared source for review. The shared component can later be integrated with mobile after source-specific attribution gates; do not claim mobile rollout here.

Screens: Agora (next activity + timing), Escala (whole month, switch to week, select day), Meteorologia, Radar, Apresentação, Pernoite. Changes/news only cycle when populated. Quick views without valid data remain accessible manually but do not consume automatic airtime. Pause holds the selected screen; reenable uses the existing reading interval. Screen care and music rules remain unchanged. Entry transforms keep content opaque; no crossfade at .2 opacity.

Source truth: uses existing canonical snapshot only. No inferred report/departure, hotel, room, aircraft model, coordinates, weather metric or news item. Pernoite filters already projected canonical stay activities, does not infer boundaries. Countdown is next canonical activity start, explicitly NOT takeoff. Radar sweep is labeled illustrative with NO live positions. Missing integrations remain unavailable. Crewtopia/Cirium logos illustrate intended credits in DEMO; actual pilot only marks Powered by Cirium when a current field states Cirium provenance. Otherwise the mark says integration planned/not connected. Never infer provider from the hashed roster sourceVersion. Exact spelling Crewtopia, not the capitalization accidentally produced by image generation.

Media: selected horizontal dark logo is an exact pixel crop of the owner's `Logo CrewCheck Nova.jpg`, with matching light crop; launcher symbol is from that same board. No invented replacement mark. These are raster crops (not vector/trademark registration verification). Partner marks use owner-uploaded images with empty black background keyed out; independent text preserves legibility. The small aircraft decoration is a crop of the approved AI-generated concept and is labeled illustrative, not a photograph of an actual assigned aircraft. Original uploaded music is unchanged. Binary media stays outside the public repo; hashes and the explicit finalize command are committed. No fonts or env files are distributable.

Finalize (all assets must match hashes): `node scripts/tv-broadcast-finalize.mjs STAGE OWNER_BRAND_DIR OWNER_MP3_DIR`, then official webOS CLI 3.2.6 `ares-package --no-minify STAGE --outdir OUTPUT`. CI stage alone is NOT an installable finalized deliverable. Package separately as `.demo` and `.pilot` version 0.1.7. Main and Render unchanged; real pairing and provider integration are not completed by this artifact. #530/#607 and DRAFT/NO MERGE remain. Physical LG Chromium 53 and speaker tests still required; desktop checks are not equivalent.
