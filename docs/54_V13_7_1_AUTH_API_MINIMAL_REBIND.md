# CrewCheck v13.7.1 — Auth API Minimal Rebind

Corrige `/api/auth/config` pendurado e login com `Failed to fetch`.

Preserva parser, motor canônico, calendário, radar, salário, diárias, Telegram/despertador e Import Guardian.
