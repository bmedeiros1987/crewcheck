name: CrewCheck hotel location separation

on:
  pull_request:
    paths:
      - 'client/src/lib/stayContext.ts'
      - 'scripts/v14388/apply.mjs'
      - 'scripts/v14389/apply.mjs'
      - 'scripts/regression-hotel-context-selection.mjs'
      - 'scripts/regression-hotel-report-location-separation.mjs'
      - '.github/workflows/hotel-location-separation.yml'
  push:
    branches:
      - main
    paths:
      - 'client/src/lib/stayContext.ts'
      - 'scripts/v14388/apply.mjs'
      - 'scripts/v14389/apply.mjs'
      - 'scripts/regression-hotel-context-selection.mjs'
      - 'scripts/regression-hotel-report-location-separation.mjs'
      - '.github/workflows/hotel-location-separation.yml'

jobs:
  hotel-location-separation:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22.13.0
      - name: Syntax
        run: |
          node --check scripts/regression-hotel-context-selection.mjs
          node --check scripts/regression-hotel-report-location-separation.mjs
      - name: Guard contextual stay selection
        run: node scripts/regression-hotel-context-selection.mjs
      - name: Guard hotel vs report/training location
        run: node scripts/regression-hotel-report-location-separation.mjs
